{
  "name": "iskcon-payment-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env dev-server.js",
    "dev:remote": "wrangler dev --remote",
    "dev:local": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.6.0",
    "razorpay": "^2.9.6"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
